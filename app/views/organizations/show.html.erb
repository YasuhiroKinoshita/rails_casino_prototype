<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @organization.name %>
</p>

<p>
  <strong>Games:</strong>
  <% @organization.games.each do |game| %>
    title:<%= game.title %>
    buy in:<%= game.buy_in %>
  <% end %>
</p>

<%= link_to 'Edit', edit_organization_path(@organization) %> |
<%= link_to 'Back', organizations_path %>
